[package]
edition = "2021"
name    = "mini_redis_demo"
version = "0.1.0"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
bytes      = "1"
mini-redis = "0.4"
tokio      = { version = "1", features = ["full"] }
